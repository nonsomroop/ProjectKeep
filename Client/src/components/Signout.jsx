import { Box, Button } from "@mui/material";
import React from "react";

function Signout() {
  const modalBox = {
    borderRadius: "5px",
  };
  return (
  <Box>
    <h2>
        Sign out
    </h2>
    <p>
        You are about to signed out of KEEP
    </p>
    <Button>

    </Button>
  </Box>
  );
}

export default Signout;
